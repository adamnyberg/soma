-- adany869 Adam Nyberg, danth407 Daniel Rapp,
-- harpe493 Harald Petterson, jonta760 Jonas Tarassu

with Ada.Strings.Unbounded; use Ada.Strings.Unbounded;

package body Parts is
  procedure Parse(Raw_Parts : in Unbounded_String, Part : out Part_Type) is
  begin
    null;
  end Parse;

  procedure Parse(Raw_Parts : in Unbounded_String, Parts : out Part_Type) is
  begin
    null;
  end Parse;

  procedure Rotate(Part : in out Part_Type; Vector : in Vector_Type) is
  begin
    null;
  end Rotate;

  procedure Traverse(Part : in out Part_Type; Vector : in Vector_Type) is
  begin
    null;
  end Traverse;
end Parts;
